package com.hellohanchen.bagua.enums;

/**
 * Character would have different types. Each type might have
 * special attributes/effects related to it.
 */
public enum CharacterType {
    EMPTY, // no valid character
    CAPTAIN, // character represent players
    GENERAL // general assist characters
}
